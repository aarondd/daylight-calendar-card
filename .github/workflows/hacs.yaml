name: HACS Action

on:
  workflow_dispatch:
  schedule:
    # Disabled: February 31st never exists, so this never fires.
    - cron: "0 0 31 2 *"

jobs:
  hacs:
    name: HACS Action
    runs-on: "ubuntu-latest"
    steps:
      - name: HACS Action
        uses: "hacs/action@main"
        with:
          category: "plugin"
